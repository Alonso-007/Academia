@model Academia.Dominio.Models.CategoriaExercicio

@{
    ViewData["Title"] = "Nova categoria";
}

<div class="container">
    <div class="row">
        <div class="collection s12">
            <div class="collection with-header">
                <h6 class="collection-header">Nova categoria de exercícios</h6>
                <form asp-action="Create">

                    <div class="collection-item">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="row">
                            <div class="input-field">
                                <input asp-for="Nome" class="validate" id="nome" />
                                <label for="Nome">Nome</label>
                                <span asp-validation-for="Nome" class="red-text darken-3"></span>
                            </div>
                        </div>

                        <div class="collection-item">
                            <input type="submit" value="Salvar" class="btn-small teal darken-3" />
                            &nbsp;
                            <a asp-action="Index" class="btn-small grey darken-1">Voltar</a>
                        </div>

                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}